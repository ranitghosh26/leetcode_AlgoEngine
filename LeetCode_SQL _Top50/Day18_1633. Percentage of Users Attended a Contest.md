# 1633. Percentage of Users Attended a Contest

<p align="center">
  <a href="https://leetcode.com/problems/percentage-of-users-attended-a-contest/" target="_blank">
    <img src="https://img.shields.io/badge/LeetCode-Problem%201633-orange?style=for-the-badge&logo=leetcode" alt="LeetCode Problem">
  </a>
</p>

## 🔗 Problem Link

👉 https://leetcode.com/problems/percentage-of-users-attended-a-contest/

## 📝 Problem Summary

Given two tables:

### Users

| Column Name | Type    |
| ----------- | ------- |
| user_id     | int     |
| user_name   | varchar |

* `user_id` is the primary key.
* Contains user information.

### Register

| Column Name | Type |
| ----------- | ---- |
| contest_id  | int  |
| user_id     | int  |

* `(contest_id, user_id)` is the primary key.
* Stores contest registrations.

### Task

Find the percentage of users registered in each contest, rounded to **2 decimal places**.

Return the result ordered by:

1. `percentage` in **descending** order.
2. `contest_id` in **ascending** order when percentages are equal.

---

## 💡 Approach

1. Count the total number of users from the `Users` table.
2. Count registered users for each contest using `GROUP BY contest_id`.
3. Calculate:

[
\text{percentage} = \frac{\text{registered users}}{\text{total users}} \times 100
]

4. Round the result to 2 decimal places using `ROUND()`.
5. Sort according to the problem requirements.

---

## ✅ MySQL Solution

```sql
SELECT 
    contest_id,
    ROUND(
        COUNT(user_id) * 100.0 /
        (SELECT COUNT(*) FROM Users),
        2
    ) AS percentage
FROM Register
GROUP BY contest_id
ORDER BY percentage DESC, contest_id ASC;
```

---

## 📊 Complexity Analysis

* **Time Complexity:** O(N)

  * One scan of the `Register` table.
* **Space Complexity:** O(1)

  * Excluding output storage.

---

## 🎯 Example Output

| contest_id | percentage |
| ---------- | ---------- |
| 208        | 100.00     |
| 209        | 100.00     |
| 210        | 100.00     |
| 215        | 66.67      |
| 207        | 33.33      |

---

### ⭐ If this solution helped you, consider giving the repository a star!

